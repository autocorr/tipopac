---
icon: material/api
---

# API reference

The public surface of `tipopac` is small: a one-shot function, a staged
class, a result container, a scan-summary helper, and the flag-masking
helper every reduction over the `time` axis must go through. These are
generated from the source docstrings.

::: tipopac.tipopac

::: tipopac.TippingAnalysis

::: tipopac.Result

::: tipopac.summary.summarize_skydip_scans

Also runnable as `python -m tipopac.summary <path>`. Re-exported at the
package top level as `tipopac.summarize_skydip_scans`.

::: tipopac.schema.apply_flags
